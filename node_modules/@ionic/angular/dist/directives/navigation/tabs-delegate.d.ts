import { ElementRef, QueryList } from '@angular/core';
import { TabDelegate } from './tab-delegate';
import { NavController } from '../../providers';
export declare class TabsDelegate {
    private navCtrl;
    tabs: QueryList<TabDelegate>;
    constructor(navCtrl: NavController, elementRef: ElementRef);
    urlForTab(tab: string): string | undefined;
    onTabbarClick(ev: any): void;
}
