import { ElementRef } from '@angular/core';
import { Router } from '@angular/router';
import { NavController } from '../../providers/nav-controller';
export declare type RouterDirection = 'forward' | 'back' | 'root' | 'auto';
export declare class HrefDelegate {
    private router;
    private navCtrl;
    private elementRef;
    routerDirection: RouterDirection;
    routerLink: any;
    href: string;
    constructor(router: Router, navCtrl: NavController, elementRef: ElementRef);
    onClick(ev: Event): void;
}
